// import { useMeta } from '@everything-dies/flesh-cage'

export const Observer = () => {
  // const { loading } = useMeta()

  // return loading && <p>Loading skin...</p>

  return <p>Loading skin...</p>
}
